- idea for non host views:
    1. use only unreconstructed area to make host view decision in first place, dont do densification after train burst
    2. if not host view, wait for loss of this view to stabilize, then decide based on SSIM if it should be promoted to a host view
    3. do densification after loss stabilised
    4. this will probably require a smarter view sampling strategy for training on the views

- train with alpha masks

- rgb image training

- decide which keyframes to use
    - possible profit from loop closure keyframe culling by loop closure
    - covisibility query (already in basalt)

- when rectifying image, maybe reduce the image size to reduce the introduced noise (also possible training speedup)
    - possible training pyramid (like PhotoSLAM)

- normal map loss (I should be able to compute a normal map using the depth map)

- implement importance sampling based pruning during densification phase
    - use laplacian of image instead of SSIM map

- run BA in background to improve poses over time
    - loop closure working: update gaussians hosted by keyframes after the keyframes got updated by loop closure
      ./build/bal --input map/map.json --solver-type SQUARE_ROOT --calibration-file $DS_CALIB --no-normalize --residual-robust-norm HUBER --no-use-double --optimized-cost ERROR_VALID --output-optimized-path map/optimized_tracking.csv --min-obs-per-kf 20 2>&1 | tee output.$DATASET.bal.log

- experiment, which images to train on during all view training phase

- optimize poses while training GS

- keep images on GPU/avoid moving images to GPU from CPU all the time

- optimize poses while training GS

- look into memory allocations for images, when is what copied or moved

- reorder gaussians spatially sometimes to improve cache performance

- depth related:
    - improve depth smoothing/filtering
    - use landmarks to improve depth
    - look into possible memory leak of foundation stereo
